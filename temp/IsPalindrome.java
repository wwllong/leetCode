package temp;

/**
 * @author Wwl
 * 回文数字
 */
public class IsPalindrome {
	
	/* 思路1：将数字转换为字符数组，倒转
	 * */
	public int method1(int x) {
		int result = 0;
		char[] cArray = Integer.toString(x).toCharArray();
		for(int x1=cArray.length-1; x1>0; --x1) {
			
		}
		
		return result;
	}
}
